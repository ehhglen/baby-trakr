import React from 'react';

class AddButton extends React.Component {
    render() {
        return (
            <div>
                <button className="addButton">This is a button</button>
            </div>
        );
    }
}

export default AddButton;